{
  "name": "dictee-musicale",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/engine.js && node --check src/app.js && node --check sw.js",
    "test": "node --test"
  }
}
